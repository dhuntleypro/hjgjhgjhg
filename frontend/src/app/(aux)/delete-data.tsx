import {DeleteSectionView} from '@appsformankind/orbit';
import { StyleSheet, View }  from 'react-native';


const deleteDataScreen = () => {
  return (
    <View style={{ padding: 20 }}>
      <DeleteSectionView />
    </View>
  );
};

export default deleteDataScreen;

const styles = StyleSheet.create({});
